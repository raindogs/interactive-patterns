{
  "name": "elastic-2d-balls",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run sync:build",
    "sync:build": "node ./scripts/sync-build.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.77.8",
    "vite": "^5.4.11"
  }
}
